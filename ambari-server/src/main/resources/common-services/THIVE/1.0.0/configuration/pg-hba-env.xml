<configuration>
  <property>
    <name>content</name>
    <value>
      <![CDATA[
# "local" is for Unix domain socket connections only
local   all             all                                     trust
# IPv4 local connections:
host    all             all             0.0.0.0/0               trust
# IPv6 local connections:
host    all             all             ::1/128                 ident
]]>
    </value>
    <description>客户端认证配置文件pg_hba.conf</description>
  </property>

</configuration>
